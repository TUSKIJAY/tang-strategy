# Review 001 — Tang Strategy Review Workspaces And Trader Point Editing

- Review target: `docs/exec-plans/proposed/2026-07-19-tang-strategy-review-workspaces-and-trader-point-editing-plan.md`
- Review target revision: `v2-review-loop-baseline-2026-07-19`
- Review type: design
- Reviewer ID: `kimi-code-0.27.0-round-1`
- Plan author ID: `codex-plan-author-2026-07-19-review-workspaces`
- Independence declaration: `attested`
- Evidence method: Verified frozen plan SHA-256 `c90406702862991511c68fb10d33e94b9a20a8b5430399bcaddd9d77101acc17` (match), then read-only inspection of the plan, `AGENTS.md`, `INSTRUCTIONS.md`, `docs/operating-modes.md`, the optimization intake, all listed frontend/backend sources, `content/trades/2026-07-17.json` and `2026-07-14.json`, `.harness/config.json`, the CI workflow command carriers, read-only SQLite counts/hashes, the visual-reference PNG hash, and local test runs (backend 76/76 OK under the pinned `backend/.venv`; frontend trade-record tests 11/11 pass). No implementation, browser, or hosted evidence is claimed.
- Verdict: revise
- Confidence: high

## Scope Checked

- Frozen plan revision `v2-review-loop-baseline-2026-07-19` (SHA-256 matched the supplied value exactly) against live repository evidence on branch `codex/project-harness`.
- Current-evidence accuracy of every §1.1 repository-fact bullet: DB SHA `125fcc9d…`, 49 market days (46 SPY / 3 QQQ), 11 active strategies, 2 traders, 33 trade groups (all confirmed read-only); flat `marketDays`/`selectedDayId` in `main.jsx:17`; first-20 mixed list in `DashboardPage.jsx:65`; single mixed `Market day` selector and duplicated `1m/5m/Back/Step/Play/Pause/Overview` plus `Backtest`/`Rescan` both bound to `runBacktest` in `ReviewPage.jsx:473,496-503`; duplicated static controls and flat mixed day list in `StaticReviewsApp.jsx:455-461,542-547`; page-level replay duplicates in `BacktestPage.jsx:123-126` and `TeachingPage.jsx:93`; engine toolbar ownership without a visible fit/overview action in `kline-engine.js:1020-1045` versus the wrapper's `overview()` in `UnifiedKlineEngine.jsx:95-103`; `TraderFilters` rendering all payload traders (payloads include every active registry trader regardless of groups, `trade_records.py:365-369`) and filter reset on payload change (`ReviewPage.jsx:232-238`); admin-only collapsible nav entry (`Layout.jsx:45-50`); raw-JSON admin editors (`AdminTradersPage.jsx:66-77`); backend readonly/admin enforcement, whole-repository validation, atomic replace, candidate projection with drift validation and rollback (`main.py:210-262,321-369`, `trade_records.py:478-535`); flat static manifest with `#<ticker>-<date>-<session>` slugs (`export_static_reviews.py:18-21,192-201`); visual reference PNG SHA `57c34ea7…` matched; QQQ 2026-07-14 pending-only and 2026-07-17 mixed SPY/QQQ verified groups confirmed in canonical content.
- Admin full-day edit/save viability, ticker/date authority, stale trader selection, chart ownership, interactive/static parity, accessibility verification, planned file surface, rollback, phase gates, and authority boundaries, per the review mandate.

## Findings

| Severity | Location | Finding | Required change |
| --- | --- | --- | --- |
| Blocking | Plan §3.4 (editor contract), Phase 3 work items; against `backend/app/services/trade_records.py:478-509` and `content/trades/2026-07-17.json` | The canonical save unit is the whole day file shared across tickers and traders (2026-07-17.json holds 1 SPY `tang` + 2 QQQ `vordin` groups), yet the editor is scoped to ticker/date/trader and the plan never states that the save candidate must be the complete day document merged from untouched existing groups/contexts plus the edit. §3.4's group-field list even omits `underlying`/`trade_date`. Server validation checks schema and repository-wide IDs only — a candidate containing just the edited ticker's groups validates cleanly, silently drops the other groups, and the projection rebuilds coherently from the truncated content, so the plan's own content/DB-coherence receipts cannot detect the loss. §6's safety matrix has no row for this. | State explicitly in §3.4/Phase 3 that the save candidate is the full canonical day document: all pre-existing groups and note contexts for that date (all tickers, all traders) carried unchanged, with edits merged in. Add a safety-matrix row and a Phase 3 acceptance receipt proving untouched group/leg/event IDs and counts survive a save (day-document group delta equals the intended edit only). |
| Non-blocking | Plan §7.1 note vs §4.2 file surface; against `.harness/config.json` and `.github/workflows/project-harness.yml:94` | The plan permits Phase 1 to rename the frontend test script and requires syncing only `.harness/config.json`, but the same `npm run test:trade-records` command is also a carrier in the CI workflow, and neither file appears in the §4.2 modification surface. §4's fail-closed stop-for-revision rule catches a workflow change, so this cannot silently corrupt verification, but the enumerated sync rule is incomplete. | Either keep the `test:trade-records` script name (broadening via an additional script), or amend §7.1/§4.2 to name both command carriers (`.harness/config.json`, `.github/workflows/project-harness.yml`) as sync targets under the same change-control rule. |

## Verdict Rationale

Evidence accuracy is strong: every checkable §1.1 claim — DB hash and counts, control duplication in all four pages, engine/wrapper split, trader-availability gap, backend atomicity contract, static manifest shape, visual-reference identity, and the 76-test backend baseline (green under the pinned venv; system-python failures are the documented missing-dependency environment condition, not a regression) — matched the live tree. Ticker/date authority (single resolved-day identity, deterministic defaults, no fabricated substitution), the stale-selection reconciliation rules (intersect, select-all-on-empty-after-real-context-change, intentional-empty preserved in-context, focus always cleared), the control-ownership table, and the static-parity boundaries (client-side grouping of the existing flat manifest, verified-only honesty, no mutation path) are all specified precisely and are implementable against the current code. Accessibility is gated as real keyboard/label/focus/announcement receipts separate from screenshot fidelity, and the plan claims no implementation, browser, or hosted evidence. Authority boundaries (review-only scope, dual matching-revision approval before lifecycle-only activation, separate implementation/push/publication gates) conform to `docs/operating-modes.md`, and the plan metadata block satisfies the constrained Proposed format.

The blocking finding is narrow but correctness-critical: the plan's headline new capability (admin point editing) can, under a letter-faithful implementation, destroy untouched same-day canonical groups without tripping any of the plan's own failure receipts. The required change is one explicit merge-contract statement plus one receipt — a bounded revision, not a redesign. Per the review mandate (approve only with no blocking change), the verdict is **revise**; the non-blocking carrier-sync gap can be folded into the same revision.
