# Review 003 — Tang Strategy Review Workspaces And Trader Point Editing

- Review target: `docs/exec-plans/proposed/2026-07-19-tang-strategy-review-workspaces-and-trader-point-editing-plan.md`
- Review target revision: `v3-round-1-review-foldback-2026-07-19`
- Review type: design
- Reviewer ID: `kimi-code-0.27.0-round-2`
- Plan author ID: `codex-plan-author-2026-07-19-review-workspaces`
- Independence declaration: `attested`
- Evidence method: Verified frozen plan SHA-256 `10fa26a14c9f9b6c51546748e28295014239507e89355201c8763f35e7e7ae69` (exact match), then read-only inspection of the plan, round-1 `review-001.md`/`review-002.md`, `docs/operating-modes.md`, backend routes and `trade_records.py` (public projection shape, `load_trader_registry`/`load_trade_day`, atomic whole-day PUT with rollback), `content/trades/2026-07-17.json` and `2026-07-14.json`, `content/traders/index.json`, frontend sources (`TraderFilters.jsx`, `ReviewPage.jsx`, `UnifiedKlineEngine.jsx`, `Layout.jsx`, `AdminTradersPage.jsx`), `export_static_reviews.py` slug/manifest, `frontend/package.json`, `.harness/config.json`, `.github/workflows/project-harness.yml`, read-only SQLite counts/hash, the visual-reference PNG hash, `PROGRESS.md`/`HANDOFF.md` state blocks, and a read-only run of `scripts/check-operating-modes.py --root .` (`errors: []`). Sibling round-2 output was not consulted. No implementation, browser, or hosted evidence is claimed.
- Verdict: approve
- Confidence: high

## Scope Checked

- Frozen revision `v3-round-1-review-foldback-2026-07-19` against live repository evidence, with foldback of every round-1 finding: review-001 blocking (complete multi-ticker day merge + preservation receipts), review-001 non-blocking (`test:trade-records` carrier sync), review-002 blocking (public projection forbidden as write base + admin-only canonical registry/day reads), review-002 non-blocking (TraderFilters ticker/date authority, admin `UnifiedKlineEngine` preview boundary, accessibility pure fixtures).
- All current contracts per the review mandate: admin-only canonical full registry/day reads, public projection as write base, complete day merge and untouched preservation receipts, ticker/date authority, stale traders, engine ownership, admin engine preview, static parity, accessibility, file surface, rollback, phases, verification, and authority boundaries.
- Current-evidence accuracy of §1.1: DB SHA-256 `125fcc9d…` with 49 days (46 SPY / 3 QQQ), 11 active strategies, 2 traders, 33 trade groups (all confirmed read-only); page-level duplicate engine controls and `Backtest`/`Rescan` both bound to `runBacktest` at `ReviewPage.jsx:496-503`; editable ticker/date selectors inside `TraderFilters.jsx:23-38`; admin-only nav at `Layout.jsx:45-48`; raw-JSON admin editors at `AdminTradersPage.jsx:69,74`; lossy public projection (`normalization` → `normalization_method` at `trade_records.py:931,937`, mandatory ticker filter at `main.py:210-234`); whole-day atomic replace with rollback at `trade_records.py:478-535,578-631`; flat static manifest with `<ticker>-<date>-<session>` slugs at `export_static_reviews.py:18-21,185-203`; visual reference SHA `57c34ea7…` matched; QQQ 2026-07-14 pending-only and the mixed SPY/QQQ 2026-07-17 day confirmed in canonical content.

## Findings

| Severity | Location | Finding | Required change |
| --- | --- | --- | --- |
| Non-blocking | Plan §3.4 group-fields bullet; against the canonical schema enforced at `backend/app/services/trade_records.py:692-694,838-852` | The form field enumeration (trade date, underlying, trader, IDs, direction, status, review status, three eligibility flags, outcomes, notes/provenance) does not explicitly name the schema-required group-level `normalization` block (`method`/`source`/`review_flags`). For edited groups the merge contract carries it byte-equivalently and the load base returns full `normalization`, and server schema validation fails closed, so there is no silent data-loss path; only new-group form construction is under-specified. | Add one sentence in §3.4 or the Phase 0 work list stating that new-group construction must supply every schema-required field, including the `normalization` block; pin it during the Phase 0 exact contract freeze. |

## Verdict Rationale

Every round-1 finding is folded back accurately and verifiably. Review-001's blocking merge finding is resolved by §3.4's full-day merge clause (clone the complete loaded day, carry every untouched ticker/trader/group/leg/event/outcome/note context byte-equivalently, never build a PUT from a filtered projection), the restored `underlying`/`trade_date` group fields, the new §6 safety-matrix row, and Phase 3's exit gate requiring exact untouched ID/value survival with the full-day group-count delta equal to only the intended edit. Review-001's carrier finding is resolved by §7.1 freezing the `test:trade-records` name and naming both `.harness/config.json` and `.github/workflows/project-harness.yml` (workflow line 94 confirmed) as carriers, with any rename routed to plan revision. Review-002's blocking write-base finding is resolved by the §1.1 evidence correction, the §2.3 non-goal naming the two admin-only canonical GET handlers as the only API additions, §3.4's canonical load base, Phase 0's frozen route contracts (`GET /api/admin/traders`, `GET /api/admin/trade-records?trade_date=<YYYY-MM-DD>`; collision-free against the existing same-path PUTs and feasible over the existing `require_admin` dependency and `load_trader_registry`/`load_trade_day` service functions), Phase 3 role/shape tests, and the §6 round-trip receipt proving the public payload fails canonical validation while the admin payload passes. The three review-002 non-blocking findings are resolved by §3.1/Phase 1 (selectors removed or readonly mirrors with a pure divergence fixture), §3.4/§2.2(9)/Phase 3 (one read-only reused `UnifiedKlineEngine` preview, no second chart, no preview save), and Phase 1/§7.2 (pure label/selected/disabled fixtures supplementing, not replacing, browser keyboard/focus/announcement acceptance).

All current contracts check out against live evidence: ticker/date authority is singular and deterministic with no fabricated substitution; stale-trader reconciliation (intersect, select-all only on real context change, intentional empty preserved in-context, focus always cleared) is precise and testable; engine ownership assigns every generic control one owner with a visible accessible fit/overview action while Rescan and Backtest become distinct; static parity reuses the existing flat manifest and legacy slugs with verified-only honesty and no mutation path; the file surface covers exactly the frontend/backend/docs files the contract implies, with a fail-closed Phase 0 manifest freeze; rollback is a scoped source revert with acceptance on temporary copies and the tracked DB/content untouched; phases 0-6 carry entry/exit gates consistent with `docs/operating-modes.md`; and the metadata block satisfies the constrained Proposed format — the read-only lifecycle checker reports `errors: []` and the state blocks reconcile. Authority boundaries remain review-only, with lifecycle-only activation gated on matching-revision dual approval and implementation/push/publication as separate gates. The single remaining item is a non-blocking clarification that cannot produce silent data loss. Per the review mandate, no blocking change remains: the verdict is **approve**.
