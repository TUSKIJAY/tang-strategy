# Implementation Review 001 — Tang Strategy Date Rail Ascending And Trade Quantity

- Review target: `docs/exec-plans/active/2026-07-22-tang-strategy-date-rail-ascending-and-trade-quantity-plan.md`
- Review target revision: `v2-review-foldback-2026-07-22`
- Review target commit: `da12e1b03715be3de75fcafd8d47aa1a35554942`
- Review type: implementation
- Reviewer ID: `kimi-impl-reviewer-2026-07-22-date-rail-quantity`
- Plan author ID: `grok-plan-author-2026-07-22-date-rail-quantity`
- Packet author ID: `kimi-executor-2026-07-22-date-rail-quantity`
- Independence declaration: `attested` (reviewer did not author the plan design or the implementation; product tree re-inspected from the frozen packet + live sources and the implementation commit)
- Evidence method: full plan/packet/Phase-0-note read; `git show da12e1b --stat` + full source diff inspection of `reviewWorkspace.js` / `tradeRecords.js`; all five new carrier tests read line-by-line; `npm run test:trade-records` re-run locally (69/69); `×` absence grep over product sources; `content/trades/2026-07-17.json` oracles cross-checked against live fixtures; receipts.json re-read; V1a/V2/V3 screenshots visually inspected; Playwright runner oracle/fail-closed logic inspected; harness auto re-run; `git diff --check` and `git status --short` boundary checks.
- Verdict: accept
- Confidence: high

## Scope Checked

Plan revision `v2-review-foldback-2026-07-22` (§1.5 user scope locks, §2.2 success criteria, §2.4 carrier matrix, §3.1 frozen manifest, §3.3 safety boundaries) against product implementation commit `da12e1b03715be3de75fcafd8d47aa1a35554942`:

- §2.2 success criteria 1–10 (ascending chips; newest-N membership; month inventory; marker `*QTY` with same-bar sum and completeness; close derivation 150/12; marker consumes derivation; fallback paths; no source write; tests/builds; receipts).
- §2.4 carrier matrix: N-Date-asc, N-Date-membership, N-Qty-derive, N-Marker-qty, N-Timeline-qty (mandatory), plus V1–V3 visual receipts.
- §3.1 frozen manifest: items 1, 2, 4, 5 modified; item 3 (`TraderTradeList.jsx`) correctly not required; item 6 (`reviewWorkspace.fixtures.js`) correctly not required (adversarial fixtures are test-local); new tracked runner `frontend/scripts/playwright/date-rail-ascending-trade-quantity-acceptance.mjs`; comment-only touch of `scripts/verify-trade-points-marker-labels.mjs`; lifecycle surfaces (plan file, `PROGRESS.md`, `HANDOFF.md`).

### What was independently verified

1. **Changed path set** — `git show da12e1b --stat` lists exactly 9 files: the four manifest code/test files, the new Playwright runner (+223), comment-only marker-labels script, plan file, `PROGRESS.md`, `HANDOFF.md`. Zero changes to `content/trades/**`, `data/sqlite/**`, `backend/**`, seed market data, or Pages workflows. `git status --short` post-commit shows only untracked `output/**` evidence trees and the review packet — no uncommitted product changes.
2. **OPT-001 chip projection** — `reviewWorkspace.js:149-151` reverses only the projected `dates` array for 最近 (after the newest-first `slice(0, recentLimit)`) and `:169-171` for 按月 (after `datesInMonth`). `datesForTicker` / `recentDatesForTicker` / `listMonthsForTicker` / `stepBrowsedMonth` / `groupDatesByMonth` untouched (diff is 8 lines, projection-only, exactly the Phase 0 frozen approach). Membership set, `PROGRESSIVE_RECENT_LIMIT`, and meta strings structurally unchanged.
3. **OPT-003 derivation correctness (adversarial mental pass)** — `deriveCloseQuantity` (`tradeRecords.js:243-268`): non-`sell_close` → null; leg events sorted by `sequence` with array-index tiebreak (`orderedLegEvents`); close located by identity or `event_id`; loop over **prior** events only — any `buy_open`/`buy_add`/`sell_partial` with non-finite qty returns null immediately (no known-subset summing); `openingCount === 0` → null (sell_close with no prior opens); `partialSum > opening` → null (over-partial); post-close events (e.g. a null `buy_add` sequenced after the close) do not block. `eventDisplayQuantity` prefers any finite raw quantity as-is before falling back to derivation, so a numeric close survives an incomplete prior chain. `finiteQuantity` uses `Number(value)` + `Number.isFinite`, so string/float quantities behave sanely. Render-only: no write path anywhere (pure functions consumed by `groupTimelineEvents` and `buildTradeRecordAnnotations`; `git grep` confirms no mutation of source records).
4. **OPT-002 marker aggregation** — per-event `quantity: eventDisplayQuantity(leg, event)` on the annotation; grouping Map key unchanged (`bar_index|trader_id|direction|action_side`); `quantity_total` sums contributors, `quantity_unknown` counts nulls; suffix `*${quantity_total}` applied to **both** `marker_label` and `title` only when `quantity_unknown === 0`, for singles and groups alike. The old `×${count}` path is deleted; `git grep "×"` over `frontend/src/features/review/` finds the character only inside test assertions checking its **absence**. Shape/color/anchor lines untouched; label text stays `${displayName} ${BUY|SELL}` with no CALL/PUT.
5. **N-carriers re-run and read** — `npm run test:trade-records` re-run by this reviewer: **69/69 pass**. All five new carriers read in full:
   - `reviewWorkspace.test.js:529` **N-Date-asc** — recent projection `2026-06-14 … 2026-07-10` strictly ascending (element-wise `<` loop, not just sorted-copy equality), pressed latest day is the last chip; month projection `2026-06-01 … 2026-06-15` ascending.
   - `reviewWorkspace.test.js:563` **N-Date-membership** — chip set equals `datesForTicker(...).slice(0, PROGRESSIVE_RECENT_LIMIT)` as a Set; meta `显示最近 12 · 全库 SPY 25` / `本月交易日 15 · 全库 SPY 25` pinned; `listMonthsForTicker` `['2026-07','2026-06']`, `stepBrowsedMonth` older/newer, `monthBar` flags unchanged.
   - `tradeRecords.test.js:454` **N-Qty-derive** — PUT leg → 150, CALL leg → 12; raw close 5 preferred over incomplete chain; unknown open, over-partial (16 > 10), null `buy_add`, null prior `sell_partial`, no-opening → null; sequence-order "prior" case (shuffled array, null post-close add does not block) asserted; non-close actions never derive.
   - `tradeRecords.test.js:546` **N-Marker-qty** — `×` and CALL/PUT absence regex on every label+title; `BUY*150`/`SELL*150` (derived), `BUY*70`, same-bar sums `SELL*24` (12+12) / `SELL*34` (22+12), derived close `SELL*12`; mixed known/unknown same-bar pair → bare `vordinkkk SELL` on both fields with `grouped_marker_count === 2`; direction shape/color/anchor pinned both directions.
   - `tradeRecords.test.js:629` **N-Timeline-qty** — `groupTimelineEvents` rows expose 150 @ 0.15 and 12 @ 5.5 with partial rows [12,12,22,12] intact; unsafe chain rows stay null; raw close 5 passthrough; source pin on `TraderTradeList.jsx` (`row.quantity ?? '?'`) with `derived` chrome absence.
6. **Live oracle cross-check** — `content/trades/2026-07-17.json` re-read by this reviewer (UTF-8): `tg_20260717_vordin_qqq_001` PUT open 150 / close null; `tg_20260717_vordin_qqq_002` CALL open 70, partials 12/12/22/12, close null. Expected 150 and 70−58=12 match the test fixtures exactly.
7. **V1–V3 receipts + screenshots** — `output/playwright/date-rail-qty-20260722023705/receipts.json`: 4 receipts, all PASS. V1-recent DOM titles `2026-07-02 … 2026-07-20` ascending with pressed `2026-07-17`; V1-month 13 chips ascending; V3 extracted rows `10:43 SELL 150 @ 0.15` and `10:01 SELL 12 @ 5.5`. Runner inspected: throws on every oracle violation, writes `failure.json` + exit 1 on error, reads chip order from live DOM `title` attributes — no self-fulfilling expectations. V1a/V2/V3 screenshots visually inspected by this reviewer: chips render ascending left-to-right with 07-17 pressed; markers show `vordinkkk SELL*150`, `SELL*12`, `SELL*34`, `SELL*24`, `BUY*150`, `BUY*70` with no `×N`; PUT markers are red triangle-down anchored top, CALL green triangle-up anchored bottom; expanded vordin cards show `SELL 150 @ 0.15` and `SELL 12 @ 5.5` with no `?`.
8. **Boundaries** — `git diff --check 4f50878..da12e1b` clean; Phase 0 note `docs/exec-plans/reviews/2026-07-22-tang-strategy-date-rail-ascending-and-trade-quantity-plan/evidence/phase-0-baseline.md` (read at review time under untracked `output/`, moved into the plan's evidence folder on 2026-07-23) exists and records baseline HEAD `4f50878`, 64/64 baseline, approach freeze, and the 8-fixture freeze matching the implemented tests. No push/PR/remote artifacts.

## Findings

| # | Severity | Finding | Disposition |
| --- | --- | --- | --- |
| 1 | note | `python scripts/check-project-harness.py --root . --profile auto` currently reports one operating-modes error: `reviews index artifact links mismatch: 2026-07-22-tang-strategy-date-rail-ascending-and-trade-quantity-plan`. Cause: the untracked `implementation-review-packet-001.md` (and now this `implementation-review-001.md`) exist in the reviews directory while `docs/exec-plans/reviews/index.md` links only `review-001`/`review-002`. Not caused by the product commit (which touches no reviews-index path); the executor's green-harness claim predates the packet landing in the directory. Plan §4 Phase 2 already owns this: the closeout commit must reconcile the reviews index (link packet + this review) and migrate the plan to `completed/`. Identical pattern to the prior cycle's review (sidebar-spacing plan, finding 1 there). | Reconcile in closeout commit; does not block accept of the product implementation. |
| 2 | note | Multi-`sell_close` legs: `deriveCloseQuantity` subtracts only prior `sell_partial` quantities, so a hypothetical second null `sell_close` on the same leg would derive the same remainder as the first (prior closes don't reduce the remainder). This matches the plan's frozen formula verbatim (`derived = opening − Σ prior partials`), and no current fixture or schema path produces two closes per leg, so it is a spec-faithful edge observation, not a defect. | None required; note for future schema evolution only. |

No P1/P2 findings. Every §2.2 criterion and every mandatory §2.4 N-* carrier was re-verified from live sources, not from packet claims.

## Verdict Rationale

- All ten §2.2 success criteria check out from independent inspection: projection-only ascending chips with newest-12 membership and unchanged month chrome (diff + N-Date-asc + N-Date-membership + V1 receipts/screenshots); `×N` fully replaced by `*QTY` on both `marker_label` and `title` with same-side same-bar sums and completeness-gated omission (diff + N-Marker-qty + V2); completeness-gated close derivation returning exactly 150 / 12 against the live-mirror fixtures with raw-preferred and all adversarial fallback paths (diff + N-Qty-derive + N-Timeline-qty + live content cross-check); markers consume derived closes (`SELL*150` / `SELL*12`, not bare `SELL` or `×1`); render-only with zero `content/trades/**` / DB / provenance changes; 69/69 carrier suite green; receipts and screenshots under untracked `output/`.
- §3.1 manifest respected exactly: items 1/2/4/5 modified as scoped, items 3/6 legitimately unused under the frozen default approach, runner + comment-only script touch + lifecycle surfaces all within authority; no backend/API/DB/content/Pages/provider paths changed.
- The single open item (finding 1) is lifecycle bookkeeping already assigned to Phase 2 closeout, not an implementation defect.

**Verdict: accept** (confidence high).

Note: this accept verdict covers only the product implementation against plan revision `v2-review-foldback-2026-07-22`. It does **not** authorize push, PR, merge, Pages publication, provider/broker actions, tracked DB/content mutation beyond the closeout lifecycle paths, or any other remote action. Closeout (plan migration to `completed/`, OPT status updates, reviews/roadmap index reconciliation, state-file updates) proceeds under its own task-scoped local-commit authority only.
