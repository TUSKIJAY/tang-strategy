# 交付物评审意见

**审核对象**：kline-engine-v2-plan.md（R1 修订版）

## 整体判断
**裁决**：approve
**置信度**：high

## 总体评价

R1 修订版完整回应了上轮评审（r001-agent01）提出的全部 12 项问题（2 严重 + 6 中等 + 4 轻微），逐项修正到位且未引入破坏性变更。更值得肯定的是，修订不仅是"补丁式修复"，而是在多个维度做了超出评审要求的主动升级：

1. **新增第二章「消费协议」**：明确定义了 `window.KlineEngine` 的挂载协议、宿主容器职责和 demo 页定位，为下游 daily-review 集成提供了无歧义的接口契约。这是上轮未验证项中指出的缺失，现已补齐。
2. **Task 5 标注渲染策略**从 DOM 气泡重建改为 Canvas pin + Hover 详情面板，这是一个架构层面的改进决策，兼顾性能和信息密度。
3. **scrollTo 契约丰富化**：支持 `{ timeframe, barIndex, highlight, center }` 对象参数和简写形式，跨时间框架跳转逻辑清晰。
4. **Task 7 验收数据双轨制**：seed_01 smoke test + full-day 390 bar 主验收，覆盖了从最小数据到真实场景的完整验证链。
5. **风险表增加概率列**和三项新风险，使风险管理从定性描述升级为半定量评估。

整体方案现已具备执行条件。

## 问题清单

### 严重问题

无。

### 中等问题

无。

### 轻微问题

1. **scrollTo 双重签名可能引发类型混淆** [修订引入]
   - 位置：Task 5「scrollTo 契约」
   - 改进建议：L252 的简写 `engine.scrollTo(142)` 与对象签名 `engine.scrollTo({...})` 共存，实现时需在函数入口做 `typeof` 判断。建议在注释中明确：「简写形式仅在不需要切换 timeframe 时使用，实现时统一转换为对象形式再处理」。这不影响可行性，仅为执行时的编码提示。

2. **full-day 测试 fixture 的 MA/VWAP 合成方式未说明** [修订引入]
   - 位置：Task 1「测试数据准备」
   - 改进建议：L185 提到「含合成 MA 和 VWAP」，但 `SPY_1min_2026-04-13.csv` 可能只有 OHLCV 原始数据，MA10/50/200 和 VWAP 需要计算生成。建议补充：「MA 使用简单移动平均，VWAP 使用累计量价/累计量标准算法」或「直接复用旧版 `teaching_segments.json` 中的计算逻辑」。

## 上轮问题逐项核查

| # | 上轮问题 | 级别 | 本轮状态 | 说明 |
|---|---------|------|---------|------|
| S1 | 教学代码占比 40% 失真 | 严重 | ✅ 已修正 | L12：修正为 "~1327 行...约 15–20% 的 JS 逻辑" |
| S2 | 缩放因子 1.08 无依据断言 | 严重 | ✅ 已修正 | L113：改为 "默认 1.08，可调范围 [1.05, 1.15]" |
| M1 | 架构描述「散落函数」简化 | 中等 | ✅ 已修正 | L12："基于全局 state 对象和注释分区的过程式架构" |
| M2 | K 线空心/实心与验证清单矛盾 | 中等 | ✅ 已修正 | L83 保留空心设计；L296–299 拆分验收为 "行为对齐旧版 + 视觉对齐 moomoo"；L299 明确 "不与旧版浅色 sandbox 做视觉对比" |
| M3 | 深色主题 UI 适配缺失 | 中等 | ✅ 已修正 | L199 增加子任务："注解气泡、OHLC overlay、进度条、控制按钮等所有 DOM 元素的深色主题样式" |
| M4 | EventBus 排在 Task 6 | 中等 | ✅ 已修正 | L179 Task 1 标题含 EventBus；L181 注明 "最先实现"；Task 6 改为 "公共 API 封装 + 集成验证" |
| M5 | 测试数据提取方式未说明 | 中等 | ✅ 已修正 | L185 明确 "编写提取脚本" + 双数据源（seed_01 + full-day） |
| M6 | 缺少性能预算 | 中等 | ✅ 已修正 | L360 增加 "16ms/帧" 预算 + 实测方案 + 超标降级策略 |
| L1 | destroy() 资源清单缺失 | 轻微 | ✅ 已修正 | L272 补充完整清单 |
| L2 | 高低点标注未标新增 | 轻微 | ✅ 已修正 | L109 标注 "新增功能，旧版无"，分配到 Task 2 |
| L3 | 键盘快捷键不完整 | 轻微 | ✅ 已修正 | L208 补充 `1`/`5` 切换，注明 "与旧版一致" |
| L4 | 风险表缺可读性风险 | 轻微 | ✅ 已修正 | L361 增加 "深色主题下小字号标签可读性不足" |

## 未验证项

- **moomoo 截图**：仍未纳入项目文件，无法交叉验证色值。建议执行前将截图存入 `docs/assets/`。（与上轮相同，非阻塞项）
- **`SPY_1min_2026-04-13.csv` 的字段完整性**：文件存在已确认，但未核查是否包含 OHLCV 全部字段以及数据是否覆盖完整交易日（390 分钟）。建议 Task 1 执行时先做 sanity check。
- **`teaching_segments.json` 与 v2 数据格式的字段映射**：与上轮相同，建议 Task 1 的提取脚本中包含格式验证逻辑。

## 裁决理由

给出 **approve** 的依据：

1. **上轮全部 12 项问题均已修正**，无遗留未修项，无拒绝修改项。
2. **修订未引入严重或中等问题**。新增的 2 个轻微问题（scrollTo 双签名、MA 合成方式）均为编码层面的实施细节，不影响方案可行性，可在执行阶段自然解决。
3. **修订超额完成**：消费协议、标注渲染策略升级、验收标准拆分、风险表完善等均超出评审要求，显著提升了方案的工程化水平。
4. **执行顺序依赖关系清晰**（L345–349），EventBus 前置后无任何 Task 间的循环依赖。

方案已具备执行条件，建议进入 Task 1。
