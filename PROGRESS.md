# Progress

## Current Status

<!-- operating-modes-state:start -->
- Current plan: `2026-07-30-tang-strategy-default-full-day-kline-viewport-plan`
- Lifecycle status: `Active`
- Current phase: `phase-0`
- Phase state: `not-started`
- Next gate: `phase-0-start`
<!-- operating-modes-state:end -->

Maintenance rule: when a plan closes, its per-step lifecycle entries move to `docs/progress-archive/` and only the final disposition summary stays here; when this file exceeds 10 body entries, archive the oldest closed entries before appending. Rules and index: [`docs/progress-archive/index.md`](./docs/progress-archive/index.md).

2026-07-30: OPT-001 from [`2026-07-30-01-review-default-full-day-kline-viewport`](./docs/optimization/2026-07-30-01-review-default-full-day-kline-viewport/2026-07-30-01-review-default-full-day-kline-viewport.md) converted to a proposed plan and then directly activated by the user's bounded-simple-change instruction as [`2026-07-30-tang-strategy-default-full-day-kline-viewport-plan`](./docs/exec-plans/active/2026-07-30-tang-strategy-default-full-day-kline-viewport-plan.md), revision `v1-active-2026-07-30`. The user explicitly skipped a separate design-review round and will direct another agent to execute. Scope is limited to Review/Static full-day first paint and Overview for truthful 1m/5m payload lengths, with manual controls and Teaching behavior preserved. Status `Active`, `phase-0:not-started`; next gate `phase-0-start`. No implementation, product source, DB/data, publication, or remote action.

2026-07-30: OPT batch [`2026-07-30-01-review-default-full-day-kline-viewport`](./docs/optimization/2026-07-30-01-review-default-full-day-kline-viewport/2026-07-30-01-review-default-full-day-kline-viewport.md) recorded as record-only intake. User evidence shows the current Review/Static first paint exposes only a latest-bar tail window while the target is the complete truthful day/session. Read-only code anchors identify the 1m 96-bar / 5m 72-bar default cap, tail-follow reset, payload-load reset, and the `Overview` label/behavior mismatch. OPT-001 remains `recorded`; no product source, plan, activation, DB/data, publication, or remote change. Next gate: none unless the user explicitly requests promotion or implementation.

2026-07-28: Published 2026-07-27 SPY/QQQ pair review with normalized trader record for Tang (09:55 EDT Put, +35% return). TradingView fetch yielded 390 1m / 78 5m bars for SPY and QQQ. Validated trade schema and candidate-built/promoted SQLite DB (104 market days, 25 trade days, 39 groups, 57 events, 7 contexts). Full battery verify 11/11 PASS.

2026-07-23: Backend suite restored to **78/78** by fixing the four failures the first full battery surfaced. The three pinned-snapshot tests now protect the 2026-07-19 cutover as a floor instead of an exact state: `test_trade_migration.py` freezes the 22 cutover trade dates as a required subset with `>=` totals (the all-content SHA-256 pin is gone — it broke on any growth), projection idempotency derives expected counts from canonical content and asserts run-to-run equality, and `test_db_safety.py` requires the candidate to carry every live market day/dataset (49+49 QQQ era) with exact agent-filter expectations scoped to `trade_date <= '2026-07-17'`. The WinError 32 was a **product bug**: `validate_projection` in `app/main.py` opened the promoted live DB via transaction-only `with connect(path)` and never closed the handle — now `contextlib.closing`; the same test-side pattern in `test_trade_records.py` also closed. Adjacent, recorded not changed: eight `with connect()` API handlers in `app/main.py` still rely on GC for handle close. Verified: backend 78/78, full battery 11/11. No data, DB, plan lifecycle, publication, or remote change.

2026-07-23: `scripts/verify.py` is now the single cross-platform verification entry point: it executes `verification_commands` from `.harness/config.json` through bash (macOS native; Git Bash on Windows) with `--list` / `--only` / `--verbose`, and that array is now the only command list — `INSTRUCTIONS.md` §Repository Checks and `HANDOFF.md` §Verification Baseline collapsed to pointers, `AGENTS.md` gained one battery line. The three lists had already drifted (`test:trade-records` and the static build existed only in config; `check-operating-modes` and `git diff --check` only in docs) — both missing commands were added to config, and `scripts/verify.py` joined the governed harness file set. A Makefile was evaluated and rejected: `make` is absent on the Windows workstation and a Makefile would be a second command list. Startup-doc warning budgets tightened (`AGENTS.md` 24.5KB → 16KB, `INSTRUCTIONS.md` 30.7KB → 12KB) so policy rule-creep is flagged early. First full battery run: **10/11 PASS**; the backend-suite failure is pre-existing (3 failures + 1 error of 78, three test files). Three failures are pinned-snapshot drift: `test_trade_migration.py` pins the 2026-07-19 cutover state (22 content days / 33 groups / 46 events / 5 note contexts + an all-content SHA-256) and `test_db_safety.py` pins `market_days == 49` (SPY-only era), but the 2026-07-22 daily publishes (`2888c65`, `0250c16`) grew content to 24 days / 36 groups / 54 events / 6 contexts and the QQQ pair contract doubled the DB to 49+49=98 days — the publish runbook never updates these pins and no post-07-20 closeout ran the backend suite. The error is Windows-only: `test_trade_records.py` backup-cleanup-failure test leaves a SQLite handle open, so `TemporaryDirectory` cleanup hits WinError 32. Recorded as adjacent issues, not fixed here. Docs/tooling only; no product behavior, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: The `PROGRESS.md` / `HANDOFF.md` role boundary is now machine-enforced instead of convention-only. `HANDOFF.md` had grown a `## Latest Completed Work` history section (10 dated bullets + 1 undated) duplicating `PROGRESS.md`, and it was already an incomplete copy — 6 of that day's 9 entries were missing — so the section was deleted, not synced; every fact in it survives in `PROGRESS.md`, `docs/progress-archive/`, or the completed-plan records (the undated `5f36d29a` line is carried by `docs/exec-plans/completed/2026-07-21-…-sidebar-spacing-…-plan.md:18` and the 2026-07-21 archive). Two checks now hold the boundary: `check-operating-modes.py` rejects any `- <YYYY-MM-DD>: ` line in `HANDOFF.md` (hard error, two unit tests, including one proving `- Last updated: <date>` does not trip it), and `check-startup-doc-budget.py` counts `PROGRESS.md` body entries directly against the ">10" archive trigger instead of using the 12,288-byte proxy its own comment admitted was a stand-in. `HANDOFF.md` byte warning retuned 38,400 → 4,096: at 4,038 bytes it had 9.5× headroom and could never fire before HANDOFF became a second `PROGRESS.md`. Rule stated once in `docs/operating-modes.md` §6; `AGENTS.md`/`INSTRUCTIONS.md` already state the split and were left alone. Known limit, recorded not fixed: the rule is a shape heuristic — undated history prose still passes, so structure (no duplicate section) does the work and the check is the ratchet. Docs/tooling only; no product behavior, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: Six closed 2026-07-23 governance/documentation entries migrated verbatim to [`docs/progress-archive/2026-07-23-governance-and-docs-housekeeping.md`](./docs/progress-archive/2026-07-23-governance-and-docs-housekeeping.md) under Archive Trigger 2 — the file stood at exactly 10 body entries, so the trigger had to clear before appending. Migrated: `PROGRESS.md` history migration, repository-wide documentation audit, receipt path normalization, `output/` retention governance, OPT mock containment, OPT batch folder renumbering. Retained here: `scripts/verify.py` and the backend-suite restoration, because both carry recorded adjacent issues. Docs-only; no source, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: Mobile OPT batch `docs/optimization/2026-07-22-02-review-mobile-chart-canvas-and-floating-filter-dock/` gained a read-only **Touch-contract Gap Evidence** subsection (11 rows, line-referenced) plus `Code evidence` cross-references under OPT-002 and OPT-003. It records that the Touch-first Interaction Contract has no implementation to extend: `touch-action`, `pointercancel`, `pointer: coarse`, `hover: hover`, `viewport-fit=cover`, and `env(safe-area-inset-*)` are zero-match under `frontend/` (excluding `node_modules`); the canvas binds exactly 8 listeners, none touch or gesture; `kline-engine.js` keeps a single `dragState` overwritten by a second pointer, an exact-rectangle annotation hit test with no slop (`:2379-2387`), an unconditional label rectangle per visible `trade_record` (`:2271-2285`), and hover-driven tooltips (`:1241`, `:2437`) unreachable on touch; breakpoints are 820/900/980/1100 with no coarse-pointer or orientation rule. Migrating the hover card to pointer events is flagged in-record as the one change that would alter desktop behavior and engage the Desktop preservation lock. OPT-001…003 remain `recorded`; `mock.html` untouched, so its pinned size/SHA-256 stands. Record-only; no product source, data, DB, plan, activation, publication, or remote change. Next gate: unchanged.

2026-07-23: OPT batch `docs/optimization/2026-07-22-02-review-mobile-chart-canvas-and-floating-filter-dock/` **record closeout**. OPT-001…003 remain `recorded`: chart-first Review/Static mobile canvas + floating Ticker/Date/Filters dock; compact/clustered trade markers with one selected short label; touch-first Pointer Events contract. Revised self-contained `mock.html` passed coarse-pointer Playwright acceptance for tap hit slop, cluster choice, tap-vs-drag cancellation, horizontal pan, vertical arbitration, `pointercancel`, two-pointer chart scale, modal sheet close paths, 360×800 / 390×844 / 430×932 portrait and 844×390 landscape. No product source, data, DB, plan, activation, Pages, or publication change. Next gate: explicit user request to promote this recorded batch to a proposed plan; otherwise none.

2026-07-22: Date Rail Ascending And Trade Quantity is **Completed**. Goal OBJECTIVE 你来全权负责执行这个plan authorized full local execution (`user-instruction:2026-07-22-execute-date-rail-ascending-and-trade-quantity-plan`). Phase 0 baseline freeze; Phase 1 product commit `da12e1b03715be3de75fcafd8d47aa1a35554942` (progressive chips ascending projection; marker `*QTY` replaces `×N` on label+title with same-side same-bar sums and completeness gating; render-only close-qty derivation 150/12 with raw-preferred + fail-closed fallbacks); packet-001 + independent `implementation-review-001: accept/high`; migrated to `completed/`; OPT-001…003 `completed` with lifecycle links. Frontend **69/69** test:trade-records; normal+static builds; harness auto; Playwright V1–V3 PASS under untracked `output/playwright/date-rail-qty-20260722023705/`. Push/PR/merge/Pages/provider/broker/DB/content remain unauthorized. Next gate: `closed`.

Older lifecycle history (per-step records of closed plans and earlier completed plans): [`docs/progress-archive/index.md`](./docs/progress-archive/index.md).
