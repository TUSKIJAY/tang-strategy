# Progress

## Current Status

<!-- operating-modes-state:start -->
- Current plan: `2026-07-22-tang-strategy-date-rail-ascending-and-trade-quantity-plan`
- Lifecycle status: `Completed`
- Current phase: `none`
- Phase state: `none`
- Next gate: `closed`
<!-- operating-modes-state:end -->

Maintenance rule: when a plan closes, its per-step lifecycle entries move to `docs/progress-archive/` and only the final disposition summary stays here; when this file exceeds 10 body entries, archive the oldest closed entries before appending. Rules and index: [`docs/progress-archive/index.md`](./docs/progress-archive/index.md).

2026-07-23: `scripts/verify.py` is now the single cross-platform verification entry point: it executes `verification_commands` from `.harness/config.json` through bash (macOS native; Git Bash on Windows) with `--list` / `--only` / `--verbose`, and that array is now the only command list — `INSTRUCTIONS.md` §Repository Checks and `HANDOFF.md` §Verification Baseline collapsed to pointers, `AGENTS.md` gained one battery line. The three lists had already drifted (`test:trade-records` and the static build existed only in config; `check-operating-modes` and `git diff --check` only in docs) — both missing commands were added to config, and `scripts/verify.py` joined the governed harness file set. A Makefile was evaluated and rejected: `make` is absent on the Windows workstation and a Makefile would be a second command list. Startup-doc warning budgets tightened (`AGENTS.md` 24.5KB → 16KB, `INSTRUCTIONS.md` 30.7KB → 12KB) so policy rule-creep is flagged early. First full battery run: **10/11 PASS**; the one failure is pre-existing — `backend/tests/test_trade_migration.py` hardcodes cutover acceptance counts (22 days / 54 trade_events / 36 groups) that no longer match current content (24 / 46 / 33; 3 failures + 1 error of 78). Recorded as an adjacent issue, not fixed here. Docs/tooling only; no product behavior, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: `PROGRESS.md` history migration: the per-step lifecycle entries of the closed date-rail plan and the full 2026-07-21 lifecycles (three completed plans + OPT batches) moved verbatim to `docs/progress-archive/2026-07-21-to-2026-07-22-plan-lifecycle-history.md`. Archive trigger rules added to `docs/progress-archive/index.md` and the maintenance rule above — the archive existed since 2026-07-19 but had no trigger condition, so nothing was ever migrated. Docs-only; no source, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: Repository-wide documentation audit for absolute machine paths, dangling cited paths, and internal contradictions. Fixed: `AGENTS.md` build-output rule reworded so it no longer forbids the governance evidence that OPT screenshots, mocks, and pinned acceptance artifacts have always kept under `docs/`; `docs/operating-modes.md` §8 absolute-path rule scoped to repository-internal paths, so the eleven historical records naming artifacts written outside the worktree stay legal; §8 states that a closed plan's run-directory reference is historical, not a broken link; `HANDOFF.md` verification baseline corrected 64/64 → 69/69 to match its own Tests line; `docs/optimization/SOP.md` legacy note now records that two mock pins (`2026-07-20-03/mockups/review-left-column.html`, `2026-07-21-09/mock.html`) were already stale before the rename. Left as-is by design: frozen historical references in completed plans and review packets to ephemeral `output/` runs and to files removed by past refactors. Docs-only; no source, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: Acceptance receipts now record repository-relative POSIX paths instead of the absolute Windows path of one developer's worktree. All five runners — the two under `scripts/` and the three acceptance runners under `frontend/scripts/playwright/` — normalize `details.path` inside `record()`, so `failure.json` inherits it too; the three committed receipts were rewritten (10 paths). Rule stated in `docs/operating-modes.md` §8. Screenshot capture still uses absolute paths — only what is serialized changes. Verified by `node --check` on all five runners and a helper round-trip; no acceptance rerun. Docs/tooling only; no product behavior, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: `output/` is now governed by `docs/operating-modes.md` §8. Browser acceptance runs write to `output/playwright/<plan-slug>-<run-id>/`; only `receipts.json` is committed, screenshots and scratch DBs are gitignored. Retention needs no separate authorization: scratch DBs die with their run, superseded runs are deleted once the cited run exists, the cited run's screenshots are deleted at plan `Completed`, and no `output/` tree survives its plan. Cleanup applied: 187 MB of `temp_acceptance*.db` and the three closed runs' screenshots deleted (206 MB → 16 MB), three `receipts.json` committed, `phase-0-baseline.md` moved from `output/` into the date-rail plan's `evidence/` folder with both citations updated. Already-tracked screenshots under `output/` stay tracked — untracking would not shrink history. `HANDOFF.md` preserve-`output/` instruction reconciled. Docs/local-artifact only; no source, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: OPT design mocks now have the same containment rule as screenshots: sibling `mockups/<descriptive-name>.html`, self-contained single file (no CDN/remote asset/build step), linked relative from the record, never at the batch-folder root or under `design/references/`; untracked `output/` drafts are working material only; a mock whose SHA-256 is pinned in a plan evidence table is frozen and iterates as `<name>-v2.html`. Stated in `docs/optimization/SOP.md`, `record-template.md`, and `index.md`. Forward-only: the three pre-existing batches with a root `mock.html` stay as they are. Docs-only; no source, data, DB, plan lifecycle, publication, or remote change.

2026-07-23: OPT batch folders now carry a two-digit daily sequence — `docs/optimization/<YYYY-MM-DD>-<NN>-<batch-slug>/`, `NN` from `01` in creation order — so same-day batches stop sorting alphabetically by slug. All 16 existing batches were renamed in their original creation order (git first-commit time; ties resolved by the existing index order, which matches those timestamps), inner record markdown renamed to match, and every markdown reference under `docs/optimization/`, `docs/exec-plans/completed/`, `docs/exec-plans/reviews/`, plus `PROGRESS.md` and `HANDOFF.md`, updated. Five hash-pinned mock/mockup HTML evidence files were left byte-identical so the SHA-256 values recorded in completed plans stay verifiable; they still print pre-rename paths in body text. Rule stated in `docs/optimization/SOP.md`, `record-template.md`, and `index.md`: assigned numbers are never renumbered, reused, or gap-closed. Docs-only; no source, data, DB, plan lifecycle, publication, or remote change. Next gate: unchanged.

2026-07-23: OPT batch `docs/optimization/2026-07-22-02-review-mobile-chart-canvas-and-floating-filter-dock/` **record closeout**. OPT-001…003 remain `recorded`: chart-first Review/Static mobile canvas + floating Ticker/Date/Filters dock; compact/clustered trade markers with one selected short label; touch-first Pointer Events contract. Revised self-contained `mock.html` passed coarse-pointer Playwright acceptance for tap hit slop, cluster choice, tap-vs-drag cancellation, horizontal pan, vertical arbitration, `pointercancel`, two-pointer chart scale, modal sheet close paths, 360×800 / 390×844 / 430×932 portrait and 844×390 landscape. No product source, data, DB, plan, activation, Pages, or publication change. Next gate: explicit user request to promote this recorded batch to a proposed plan; otherwise none.

2026-07-22: Date Rail Ascending And Trade Quantity is **Completed**. Goal OBJECTIVE 你来全权负责执行这个plan authorized full local execution (`user-instruction:2026-07-22-execute-date-rail-ascending-and-trade-quantity-plan`). Phase 0 baseline freeze; Phase 1 product commit `da12e1b03715be3de75fcafd8d47aa1a35554942` (progressive chips ascending projection; marker `*QTY` replaces `×N` on label+title with same-side same-bar sums and completeness gating; render-only close-qty derivation 150/12 with raw-preferred + fail-closed fallbacks); packet-001 + independent `implementation-review-001: accept/high`; migrated to `completed/`; OPT-001…003 `completed` with lifecycle links. Frontend **69/69** test:trade-records; normal+static builds; harness auto; Playwright V1–V3 PASS under untracked `output/playwright/date-rail-qty-20260722023705/`. Push/PR/merge/Pages/provider/broker/DB/content remain unauthorized. Next gate: `closed`.

Older lifecycle history (per-step records of closed plans and earlier completed plans): [`docs/progress-archive/index.md`](./docs/progress-archive/index.md).
